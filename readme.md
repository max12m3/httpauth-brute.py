Make sure you have pip installed, and that you have configured the necessary parameters in the script for your testing.

To install:
	sudo ./install.sh

ToDo:
	Add ability to specify arguments and TOR support

@dustyfresh
